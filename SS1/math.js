const sum = (a, b) => a + b

const chuvihinhtron = (r) => {
    return 2 * Math.PI * r
}

const dientichhinhtron = (r) => {
    return Math.PI * (r ** 2)
}

const multiply = (a, b) => {
    return a * b
}

export { sum, chuvihinhtron, dientichhinhtron, multiply }

const NAME = "Nguyễn Thành Long"

export default NAME